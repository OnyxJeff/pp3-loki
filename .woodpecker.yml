labels:
  device: loki

steps:
  - name: uname
    image: alpine
    commands:
      - echo "Running test on Loki"
      - uname -a
    when:
      labels:
        linux: arm64
      event:
        - push